import { NumValidatorDirective } from './num-validator.directive';

describe('NumValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new NumValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
